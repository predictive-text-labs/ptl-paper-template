# Copy to .env (gitignored, never committed) and fill in — or skip the local
# file entirely and inject at runtime, e.g. `doppler run -- rewrite fanout`.

# Gemini AI Studio key, used by `rewrite fanout` (the SDK also accepts
# GOOGLE_API_KEY). Do not export it in your shell profile.
GEMINI_API_KEY=
